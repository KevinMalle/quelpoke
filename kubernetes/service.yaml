apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app.kubernetes.io/name: quelpoke-kevinm
  ports:
    - name: name-of-service-port
      protocol: TCP
      port: 80
      targetPort: 8080
